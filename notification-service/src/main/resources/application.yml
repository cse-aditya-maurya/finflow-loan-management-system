spring:
  application:
    name: NOTIFICATION-SERVICE

  # ─────────────────────────────────────
  # 📧 Email (SMTP) Configuration
  # ─────────────────────────────────────
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: mishraamresh2345@gmail.com
    password: tvubwytogyplfzuj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        connectiontimeout: 5000
        timeout: 5000
        writetimeout: 5000

  config:
    import: optional:configserver:http://localhost:8888

server:
  port: 8085